import { PageShell } from "@/components/layout/page-shell";

export default function LivrosPorAutorReportPage() {
  return (
    <PageShell
      title="Relatorio de livros por autor"
      description="Modulo reservado para o relatorio baseado na view vw_livros_por_autor."
    />
  );
}
